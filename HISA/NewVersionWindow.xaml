<Window x:Class="HISA.NewVersionWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Update Available"
        Height="420"
        Width="620"
        MinHeight="360"
        MinWidth="520"
        ResizeMode="CanResizeWithGrip"
        Loaded="Window_Loaded"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource UiBackgroundBrush}"
        Style="{StaticResource ModernWindowStyle}">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardBorderStyle}" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="New Version" Style="{StaticResource FieldLabelTextStyle}" />
                    <TextBlock Text="{Binding NewVersion}" FontSize="14" Foreground="{DynamicResource UiTextPrimaryBrush}" />
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock Text="Current Version" Style="{StaticResource FieldLabelTextStyle}" />
                    <TextBlock Text="{Binding CurrentVersion}" FontSize="14" Foreground="{DynamicResource UiTextPrimaryBrush}" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}" Padding="12" Margin="0,0,0,12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="Release Notes" Style="{StaticResource SectionHeaderTextStyle}" Margin="0,0,0,8" />
                <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                    <TextBlock Text="{Binding ReleaseInfo}" TextWrapping="Wrap" Foreground="{DynamicResource UiTextPrimaryBrush}" />
                </ScrollViewer>
            </Grid>
        </Border>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center">
                <Hyperlink NavigateUri="{Binding ReleaseURL}" RequestNavigate="Hyperlink_RequestNavigate">Open download page</Hyperlink>
            </TextBlock>

            <Button Grid.Column="1" Content="Close" IsCancel="True" Width="96" Style="{StaticResource GhostButtonStyle}" />
        </Grid>
    </Grid>
</Window>
