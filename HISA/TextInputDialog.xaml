<Window x:Class="HISA.TextInputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input"
        Height="210"
        Width="460"
        MinHeight="190"
        MinWidth="400"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource UiBackgroundBrush}"
        Style="{StaticResource ModernWindowStyle}">
    <Grid Margin="16">
        <Border Style="{StaticResource CardBorderStyle}" Padding="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="PromptText" Grid.Row="0" Margin="0,0,0,8" TextWrapping="Wrap" Foreground="{DynamicResource UiTextPrimaryBrush}" />
                <TextBox x:Name="InputBox" Grid.Row="1" Style="{StaticResource ModernTextBoxStyle}" />

                <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
                    <Button Content="OK" Width="84" Margin="0,0,8,0" IsDefault="True" Click="Ok_Click" Style="{StaticResource GoldButtonStyle}" />
                    <Button Content="Cancel" Width="84" IsCancel="True" Click="Cancel_Click" Style="{StaticResource GhostButtonStyle}" />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
