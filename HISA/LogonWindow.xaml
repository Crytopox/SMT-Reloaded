<Window x:Class="HISA.LogonWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Authentication"
        Height="190"
        Width="430"
        MinHeight="180"
        MinWidth="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Closed="Window_Closed"
        Background="{DynamicResource UiBackgroundBrush}"
        Style="{StaticResource ModernWindowStyle}">
    <Grid Margin="16">
        <Border Style="{StaticResource CardBorderStyle}" Padding="14">
            <StackPanel>
                <TextBlock Text="Browser Login In Progress" Style="{StaticResource SectionHeaderTextStyle}" />
                <TextBlock Text="Use the browser tab that opened to authorize each character." Style="{StaticResource SubtitleTextStyle}" />
                <TextBlock Text="When done, close this window." Margin="0,8,0,0" Foreground="{DynamicResource UiTextMutedBrush}" />
                <ProgressBar Margin="0,12,0,0" IsIndeterminate="True" Height="5" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
